<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anand.allapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAPpTheme">
        <activity android:name=".ShowDBList2"></activity>
        <activity android:name=".ShowDB2" />
        <activity android:name=".ShowDBList" />
        <activity android:name=".FormActivity" />
        <activity android:name=".DBConnect" />
        <activity android:name=".MoreButtons" />
        <activity android:name=".ActivityList" />
        <activity android:name=".Activity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>